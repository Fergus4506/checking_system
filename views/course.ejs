<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= course.name %> 報名頁面</title>
    <script>
        // 抓取今日年月日
        const today = new Date();
        const year = today.getFullYear();
        const month = today.getMonth() + 1;
        const date = today.getDate();
        const todayStr = `${year}-${month}-${date}`;
        console.log(todayStr);
        
        async function getCookie(name) {
            let cookieArr = document.cookie.split(";");
            for (let i = 0; i < cookieArr.length; i++) {
                let cookiePair = cookieArr[i].split("=");
                if (name == cookiePair[0].trim()) {
                    const response = await fetch('/check', {
                        method: 'POST',
                        headers: { 'Content-Type': 'application/json' },
                        body: JSON.stringify({ token: cookiePair[1] , courseId: "<%= course.id %>"})
                    });
                    if (response.ok) {
                        return true;
                    } else {
                        return false;
                    }
                }
            }
            return null;
        }

        async function checkCookie() {
            let userCookie = await getCookie("cls<%= course.id %>_user");
            console.log("cls<%= course.id %>_user");
            if (userCookie) {
                document.getElementById("message").innerText = "簽到成功";
                document.getElementById("signupForm").style.display = "none";
            } else {
                document.getElementById("message").innerText = "";
                document.getElementById("signupForm").style.display = "block";
            }
        }

        window.onload = checkCookie;
    </script>
</head>
<body>
    <h1 id="message"></h1>
    <div id="signupForm" style="display:none;">
        <h2><%= course.name %> 報名頁面</h2>
        <form>
            <label for="name">名稱:</label>
            <input type="text" id="name" name="name" required><br><br>
            <label for="phone">電話:</label>
            <input type="tel" id="phone" name="phone" required><br><br>
            <input type="hidden" id="courseId" name="courseId" value="<%= course.id %>">
            <input type="submit" value="提交">
        </form>
    </div>
    <script>
        document.getElementById('signupForm').addEventListener('submit', async (e) => {
            e.preventDefault();
            const username = document.getElementById('name').value;
            const phone = document.getElementById('phone').value;
            const courseId = document.getElementById('courseId').value;
            console.log(username, phone, courseId);
            try {
                const response = await fetch('/register', {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify({ username, phone, courseId })
                });
                if (response.ok) {
                    const data = await response.json();
                    const token = data.token;
                    document.getElementById("message").innerText = "新參加者報名成功";
                    document.getElementById("signupForm").style.display = "none";
                    // 將課程報名的 token 存入 cookie
                    document.cookie = `cls<%= course.id %>_user=${token}`;
                } else {
                    const error = await response.text();
                    alert('報名失敗：' + error);
                }
            } catch (error) {
                console.error('Error:', error);
                alert('報名失敗');
            }
        });
    </script>
</body>
</html>